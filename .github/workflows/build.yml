name: "Build"

on:
  push:
    branches:
      - main
      - "feature/**"
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Frontend Build
    uses: ISTEAMX/IS-DevOps/.github/workflows/continuous-build-frontend.yml@main
    with:
      node-version: "22"

