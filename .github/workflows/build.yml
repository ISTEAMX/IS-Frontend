name: "Build"

on:
  push:
  pull_request:

jobs:
  build:
    name: Frontend Build
    uses: ISTEAMX/IS-DevOps/.github/workflows/continuous-build-frontend.yml@main
    with:
      node-version: "22"

