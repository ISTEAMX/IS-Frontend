import { PageHeader } from "@/components/ui/PageHeader";

const Subjects = () => {
  return (
    <>
      <PageHeader title="Discipline" onAddClick={() => {}} />
    </>
  );
};

export default Subjects;
