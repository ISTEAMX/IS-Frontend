import { PageHeader } from "@/components/ui/PageHeader";

const Teachers = () => {
  return (
    <>
      <PageHeader title="Profesori" onAddClick={() => {}} />
    </>
  );
};

export default Teachers;
